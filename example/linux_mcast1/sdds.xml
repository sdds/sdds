<project
    name = "linux_mcast1"
    script = "sdds.gsl"
    endian = "little"
    os = "linux"
    >
    Linux multicast discovery example with beta topic.

    <linux iface = "eth0" ip = "fe80::219:99ff:fede:9b09" port = "23234" />

    <!--
        Includes are processed first, so XML in included files will be
        part of the XML tree
    -->
    <include filename = "../topics/beta.xml" />

    <role topic = "beta" type = "publisher" />
</project>

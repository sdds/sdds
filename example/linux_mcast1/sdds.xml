<project
    name = "linux_mcast1"
    script = "sdds.gsl"
    endian = "little"
    os = "linux"
    iface = "eth0"
    ip = "fe80::a00:27ff:feb5:8352"
    port = "23234"
    >
    Linux multicast discovery example with beta topic.

    <!--
        Includes are processed first, so XML in included files will be
        part of the XML tree
    -->
    <include filename = "../topics/ipc.xml" />

    <role topic = "ipc" type = "publisher" />
</project>
